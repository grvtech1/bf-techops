terraform {
  backend "s3" {
    bucket       = "REPLACE_TERRAFORM_STATE_BUCKET"
    key          = "merchant-platform/production/terraform.tfstate"
    region       = "ap-south-1"
    encrypt      = true
    use_lockfile = true
  }
}

